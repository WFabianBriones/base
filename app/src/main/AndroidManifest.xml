<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================== -->
    <!-- PERMISOS NECESARIOS -->
    <!-- ============================================== -->

    <!-- Permisos básicos -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ⚠️ CRÍTICO: Permiso para notificaciones en Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ⚠️ CRÍTICO: Permiso para recibir evento de arranque del dispositivo -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- ⚠️ IMPORTANTE: Permiso para programar alarmas exactas (Android 12+ / API 31+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- ============================================== -->
    <!-- QUERIES PARA GOOGLE SIGN-IN -->
    <!-- ============================================== -->
    <queries>
        <package android:name="com.google.android.gms" />
        <intent>
            <action android:name="com.google.android.gms.auth.api.signin.SIGN_IN" />
        </intent>
    </queries>

    <!-- ============================================== -->
    <!-- CONFIGURACIÓN DE LA APLICACIÓN -->
    <!-- ============================================== -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Uleammed"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="31">

        <!-- ============================================== -->
        <!-- ACTIVIDAD PRINCIPAL -->
        <!-- ============================================== -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Uleammed">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ============================================== -->
        <!-- BROADCAST RECEIVERS -->
        <!-- ============================================== -->

        <!-- ⚠️ CRÍTICO: Receiver para manejar descarte de notificaciones -->
        <!-- Sincroniza el badge cuando el usuario desliza una notificación -->
        <receiver
            android:name=".notifications.NotificationDismissReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.uleammed.NOTIFICATION_DISMISSED" />
            </intent-filter>
        </receiver>

        <!-- ⚠️ CRÍTICO: Receiver para reiniciar notificaciones tras reinicio -->
        <!-- Este componente reprograma todas las notificaciones cuando se reinicia el dispositivo -->
        <receiver
            android:name=".notifications.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- ============================================== -->
        <!-- GOOGLE SERVICES -->
        <!-- ============================================== -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

    </application>

</manifest>